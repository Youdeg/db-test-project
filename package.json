{
  "dependencies": {
    "@prisma/client": "^4.12.0",
    "axios": "^1.3.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^4.12.0",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "start": "npx tsc -b && npx prisma generate && node index.js",
    "prisma:migrate": "npx prisma db push",
    "prisma:studio": "npx prisma studio"
  }
}
